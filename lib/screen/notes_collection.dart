import 'package:flutter/material.dart';

class NotesCollection extends StatelessWidget {
  const NotesCollection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
